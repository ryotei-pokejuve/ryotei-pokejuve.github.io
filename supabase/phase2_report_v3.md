# Phase 2 実施報告 v3（ブラウザ実機での動作確認 完了）

`phase2_report_v2.md`で保留となっていた「SQL適用後の動作確認」「CDN/SRIの実ブラウザ確認」「追加の動作確認」を、りょてい氏が実際のブラウザ(開発者ツール使用)で確認した。すべて合格。

---

## 1. phase2_patch.sql の本番適用

- 本番のSQL Editorで実行済み
- `search.html`のパック絞り込みセレクトに「テストパック」が選択肢として表示され、選択すると該当カードのみに絞り込まれることを確認

---

## 2. CDN / SRI（開発者ツールで確認）

- Consoleタブ: エラーなし（`integrity mismatch`／`supabase is not defined`／CSPによる`Refused to`のいずれも発生なし）
- Sourcesタブ: `cdn.jsdelivr.net`配下から`supabase-js@2.112.3`が正常に読み込まれていることを確認
- Networkタブ: `supabase.js`を含む全リクエストが`200`（`search_cards`／`list_card_sets`のRPC呼び出しも`200`）

---

## 3. 追加の動作確認（すべて合格）

| # | 項目 | 結果 |
|---|---|---|
| 1 | 素早く異なる検索語を入力しても最後の検索結果が残る | ✅ 「テ」→「テスト」と続けて入力し、最終的に「テストリーリエ」が正しく表示された |
| 2 | API通信を失敗させると古い結果が消え、再試行ボタンが表示される | ✅ Networkタブを「Offline」にして検索→エラー表示＋「もう一度試す」ボタンが出現。「No throttling」に戻して再試行→正常に結果表示 |
| 3 | 詳細から戻ると検索語・パック・レアリティ・ページ・スクロール位置が復元される | ✅ |
| 4 | 320px幅で横スクロールが発生しない | ✅ `search.html`／`card.html`両方で確認 |
| 5 | 特殊文字（`<b>テスト&"'</b>`）を含む名前がHTMLとして解釈されない | ✅ `search.html`のカードタイル・`card.html`の詳細見出しの両方で、太字化されず記号付きのテキストとしてそのまま表示された |
| 6 | `javascript:`スキームの不正な画像URLがプレースホルダーになる | ✅ `search.html`／`card.html`両方で「NO IMAGE」表示に切り替わった（読み込み・実行されない） |

テスト後、SQLで一時的に書き換えたテストデータ（名前・画像URL）は元の状態（`テストリーリエ` / `image_url = null`）に復元済み。

---

## 4. 確認したいこと

CODEXから提示された確認項目（CDN/SRI・追加の動作確認）をすべてクリアした。これでPhase 2を正式完了としてよいか、最終確認をお願いする。
